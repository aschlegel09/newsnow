 $('.teaser').each(function (i, element) {
      var category = $('.teaser-category');
      var categoryText = category.text();

      var title = $('.teaser-info');
      var titleText = title.text();

      var time = $('.teaser-blurb');
      var timeText = time.text();

      var link = $('.teaser-info a');
      var linkText = link.text();

      var image = $('.image-wrapper');
      var imageText = image.text();

      var article = {
        category: categoryText,
        title: titleText,
        time: timeText,
        link: linkText,
        image: imageText
      };

      console.log(article);
